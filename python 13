Q13. Python program to print even lengths words in a string
def print_even_length_words(string):
    words = string.split()  # Split the string into words

    for word in words:
        if len(word) % 2 == 0:  # Check if the length of the word is even
            print(word)

# Test the function
input_string = input("Enter a string: ")
print("Even length words in the string:")
print_even_length_words(input_string)

OUTPUT: Enter a string: python is good
Even length words in the string:
python
is
good
